== A test Rails app to play around with ElasticSearch using the searchkick gem.

You will need to install ElasticSearch on your development system.

On a Mac:
- <tt>brew install elasticsearch</tt>
- Then follow instructions to start elasticsearch at login and run it now.
